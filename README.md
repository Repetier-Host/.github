# .github
Repetier-Host talks to a printer you already operate, with the model you are allowed to print sitting on the plate you named. Repetier-Host keeps that job on the PC you use, so the next print starts from the same file instead of a fresh unnamed guess today.
